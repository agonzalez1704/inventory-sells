# dependencies
/node_modules

# next.js
/.next
/out

# production
/build

# misc
.DS_Store
*.log

# env — every variant except the checked-in template. The old rules were `.env`
# and `.env*.local`, which let a file like `.env.prod` (real Clerk secret key)
# sit untracked-but-not-ignored, one `git add -A` from being published.
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# InsForge (contains admin api key)
.insforge

# editor / agent
.serena
.vercel

# Local preview config
.claude/
